{
  "type": "object",
  "additionalProperties": false,
  "required": ["esito", "passo_id", "sintesi", "file_toccati", "verifiche", "blocchi"],
  "properties": {
    "esito": {"type": "string", "enum": ["completato", "parziale", "bloccato"]},
    "passo_id": {"type": "string"},
    "sintesi": {"type": "string", "description": "Che cosa è stato fatto, in una o due frasi"},
    "file_toccati": {"type": "array", "items": {"type": "string"}},
    "verifiche": {"type": "string", "description": "Checker e test eseguiti, con esito"},
    "blocchi": {"type": "array", "items": {"type": "string"}}
  }
}
