I'll review the shrink-effects bench read-only: the report, the four scripts, and the `r1`–`r4` outputs, looking for a manufactured gain.The k=64 gain is not safe to submit: on every holdout except CD4, shrinkage turns `cd4_mix` into a positive-weight vote for zero, and that is where the large gains sit.

**Confirmed in code — `cd4_mix` has no standard errors, and the bench treats that as a measured zero.** Stage 98 stores `cd4_mix`’s SE as all NaN (`scripts/98_multisource_effects.py:126-127`). `zshrink_table` refuses that case, because `z_shrink` would return 0 and `mix` would count it as a vote for no effect (`src/vcc2026/multisource.py:55-67`, `200-204`). `shrink_sweep.table` does not use that guard (`shrink_sweep.py:37-38`): `se > 0` is false for NaN, so every finite raw entry becomes 0. Zeros are finite, so `mix` adds them to the weight sum. `noise_sim.py:77-79` and `controls.py:53-56` call the same function. Only the CD4 holdout is clean, because CD4 is the truth there, not a predictor (`analyze.py:143`). The cache path `shrunk_g1` is the legal k=4 mixture of the three CD4 conditions; its gains are about +0.013 to +0.021 (`r1/measurements.json`), not the k=64 table.

**Confirmed — table 2 is not the same targets.** `has` is computed per variant (`shrink_sweep.py:95-97`). In `r2/variants.csv`, raw vs every `zk*`: K562 271 vs 257, HCT116 265 vs 242, HEK293T 278 vs 254, CD4 292 vs 292. The dropped rows are the ones the zero-vote wipes out. `r4` and `r5` keep the raw mask (`noise_sim.py:82-83`: 271 / 292 / 265 / 278). On that matched set the proxy gap remains (`r5/measurements.json`): zk64 − raw = +0.028 / +0.032 / +0.056 / +0.099, but K562 and both Orions still include the zero vote. The two Orion rows also share one predictor pair (`analyze.py:39,143`), so they are not two experiments.

**Cleared.** Held-out effects do not enter the predictors: the other family is excluded, and weights are official basal CPM. Truth `nan_to_num` (`analyze.py:83`) does not by itself raise a rank score when the hole is a whole gene column, and a row scale cancels in the cosine. `r5/shuffled.csv` puts shuffled zk64 at 0.49–0.50, so sparsity does not lift the metric. Midrank ties match the stated scorer (`analyze.py:89-91`). Gene weights `x/(1+x)` are the small-effect derivative of the log1p map, not a sparsity bonus. Noise is added only to the prediction, with SD independent of effect size (`noise_sim.py:92-101`); that hurts small effects until the amplitude rescale.

**q99 rule — partly unfair, not the Orion gap.** `0.229091` matches `configs/recipes/t17.json` and the audit file, not a number invented here. After rescaling, raw at amplitude 0.394 still has median q99 0.184 (CD4) and 0.213 (K562) versus 0.229 for the shrunk arms (`r4/measurements.json`). Orion raw is already 0.230, so the big Orion gaps are not an amplitude mismatch. The report’s “doubling q99 adds nothing” is false for K562: zk64 goes 0.7546 → 0.7667 and zk16 0.7396 → 0.7562.

**Arithmetic, all against `r1`–`r4` JSON.** CD4 raw 0.6677 → report 0.668. HEK zk64 − raw = 0.123 exactly. K562 raw at 0.394 = 0.713. CD4 zk64 − raw-at-0.394 = 0.6823 − 0.6559 = +0.026. HCT cache-shrunk 0.7005 → report 0.701. Inner Spearman range 0.326–0.419 matches “0.33–0.42”. The cross-source “0.46–0.70” is not in any output.

**Control that would most reduce the risk.** Rebuild CD4 with `zshrink_mixture` (shrink the three conditions, which have SEs, then average), shrink K562 and the Orions with `zshrink_table`, and score every variant on one target mask. Kill the claim if zk64 − raw on that run is within the shuffle spread (about 0.03 in `r5/shuffled.csv`) on the holdouts, or if the Orion gaps fall to the clean CD4-holdout size and then lose to the best raw amplitude in the noise model. I did not rerun that, and I did not open the npz arrays.
