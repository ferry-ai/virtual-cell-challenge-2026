# Heavy data and artifacts (they live in data_root, outside the repo)
*.h5ad
*.vcc
*.zip
*.npz
*.pt
*.ckpt
data/
predictions/
models/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.runtime-deps/

# Secrets — the VCC token must never be committed
.env
*.token
secrets/

# Daily chain: the lock exists only while a run is in progress
reports/ciclo_giornaliero/*/.lock

# Personal Claude Code permissions; the shared skills in .claude/skills are versioned
.claude/settings.local.json

# OS
Thumbs.db
desktop.ini
